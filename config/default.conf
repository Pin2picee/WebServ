# ============================================
# EXEMPLE DE FICHIER DE CONFIGURATION WEBSERV
# Conforme au sujet 42 (serveurs + locations)
# ============================================

# ------------------------------
# SERVEUR 1 : site principal
# ------------------------------
server {
    # Adresse et port d'écoute
    listen 127.0.0.1:8080;
    server_name localhost www.localhost;

    # Répertoire racine du site
    root /var/www/site1;
    index index.html index.htm;

    # Taille maximale du body d'une requête client
    client_max_body_size 10M;

    # Pages d'erreurs personnalisées
    error_page 404 /errors/404.html;
    error_page 500 /errors/500.html;

    # --------------------------
    # LOCATION : /
    # --------------------------
    location / {
        methods GET POST;
        root /var/www/site1/www;
        index index.html;
        autoindex off;
    }

    # --------------------------
    # LOCATION : /upload
    # --------------------------
    location /upload {
        methods POST;
        upload_enabled on;
        upload_store /var/www/site1/uploads;
        autoindex on;
    }

    # --------------------------
    # LOCATION : /redirect
    # --------------------------
    location /redirect {
        methods GET;
        return 301 http://127.0.0.1:8081;
    }

    # --------------------------
    # LOCATION : /cgi-bin
    # --------------------------
    location /cgi-bin {
        methods GET POST;
        cgi_extension .py;
        cgi_path /usr/bin/python3;
        root /var/www/site1/cgi;
    }
}

# ------------------------------
# SERVEUR 2 : blog personnel
# ------------------------------
server {
    listen 127.0.0.1:8081;
    server_name blog.local myblog.local;

    root /var/www/blog;
    index home.html;
    client_max_body_size 5M;

    error_page 404 /errors/404.html;

    # --------------------------
    # LOCATION : /
    # --------------------------
    location / {
        methods GET;
        root /var/www/blog/www;
        index home.html;
        autoindex off;
    }

    # --------------------------
    # LOCATION : /images
    # --------------------------
    location /images {
        methods GET;
        root /var/www/blog/images;
        autoindex on;
    }

    # --------------------------
    # LOCATION : /contact
    # --------------------------
    location /contact {
        methods GET POST;
        root /var/www/blog/forms;
        index contact.html;
    }

    # --------------------------
    # LOCATION : /cgi
    # --------------------------
    location /cgi {
        methods POST;
        cgi_extension .php;
        cgi_path /usr/bin/php-cgi;
        root /var/www/blog/cgi;
    }
}

# ------------------------------
# SERVEUR 3 : site d'upload dédié
# ------------------------------
server {
    listen 0.0.0.0:8082;
    server_name upload.local;

    root /var/www/upload;
    index index.html;

    client_max_body_size 20M;

    error_page 403 /errors/403.html;
    error_page 404 /errors/404.html;

    # --------------------------
    # LOCATION : /
    # --------------------------
    location / {
        methods GET;
        index index.html;
        autoindex off;
    }

    # --------------------------
    # LOCATION : /files
    # --------------------------
    location /files {
        methods GET POST DELETE;
        upload_enabled on;
        upload_store /var/www/upload/files;
        autoindex on;
    }
}
